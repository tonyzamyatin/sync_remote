# === CONFIGURATION (modify as needed) =========================================
REMOTE="my_remote"              # SSH alias for the remote server
REMOTE_ROOT=""                  # under ~ on the remote
LOCAL_ROOT="$HOME"              # under $HOME on the local

